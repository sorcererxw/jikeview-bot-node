module.exports = {
    bot_token: `The bot token that provided by @BotFather`,
    storage_chat: `
    This bot would send video file to a Telegram account to get the video file id.
    So, you need set this chat id in this field.
    How to get this chat id:
    1. Create a chat with this bot (for example: @example_bot)
    2. Send a message to @example_bot
    3. run: curl https://api.telegram.org/{bot_token}/getUpdates
    4. You will find the chat_id in response
    `
}